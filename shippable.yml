language: none
env:
   global: 
      - G1=foo
      
services:
 - mongodb
 
addons: 

infra:
   pre_prov:
      - echo 'true'

   prov:
      - terraform apply   # default command for terraform

   post_prov:
      - ls -al
