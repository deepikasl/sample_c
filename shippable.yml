language: c

compiler:
  - gcc
  - clang

build:
  pre_ci:
    - echo 'true'

  ci:
    - ./configure && make && make install
    - ./hello

  post_ci:
    - echo "true6"

integrations:
  notifications:
    - integrationName: slack-5148
      type: slack
      recipients:
      - "#test-5148"
      on_success: change
      on_failure: always
      on_start: always
      on_pull_request: never
