language: python
env:
   global: 

addons: 

infra:
   pre_prov:
      - echo 'true'

   prov:
      - terraform apply   # default command for terraform

   post_prov:
      - ls -al

   on_success: 
   on_failure:

integrations:
   deply:
   hub:
   notifications:
   key:
