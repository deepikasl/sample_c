language: c

compiler:
  - gcc
  - clang

build:
  pre_ci:
    - echo 'true'

  ci:
    - ./configure && make && make install
    - ./hello

  post_ci:
    - echo "true"

integrations:
  notifications:
    - integrationName: hipchat-test
      type: hipchat
      recipients:
        - "#Ship-aye"
        - "@ankul"
