language: none
env:
   global: 
      - G1=foo
      
matrix:
  exclude:
    - rvm: 1.9.2
 
addons:
   firefox: "21.0"

infra:
   pre_prov:
      - echo 'true'

   prov:
      - terraform apply   # default command for terraform

   post_prov:
      - ls -al
